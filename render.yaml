services:
  - type: web
    name: oil-spill-detection
    runtime: python
    buildCommand: pip install -r requirements-render.txt
    startCommand: gunicorn src.web.app:create_app() --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
